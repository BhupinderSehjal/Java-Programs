#include<stdio.h>

int main()
{

    int length, breadth;

    printf("Enter the length of ractangle");
    scanf("%d", &length);

    printf("Enter thr Breadth of the Rectangle");
    scanf("%d", &breadth);

    printf("The area of te Rectangle is : %d", length*breadth);

    return 0;
    
}